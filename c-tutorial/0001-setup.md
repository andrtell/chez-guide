[Up]() [Next]() [Prev]()

----

Basic tools

```
$ apt install binutils build-essential gdb valgrind doxygen git
```

Useful libraries

```
$ apt install libcurl4-openssl-dev libglib2.0-dev libgsl-dev \
              libsqlite3-dev libxml2-dev libcurses-dev
```
